# user_loader = lambda username, password: { 'username': username }
# # print(user_loader)

# test = user_loader("user", "test")
# print(test)


def user_loader(payload):
    # if user.id == payload['user']['id']:
    #     return user
    return None